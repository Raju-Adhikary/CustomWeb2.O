(function (g, n, f) {
    typeof exports === 'object' && typeof module !== 'undefined' ? module.exports = f():
    typeof define === 'function' && define.amd ? define(f):
    (g = typeof globalThis !== 'undefined' ? globalThis: g || self, g[n] = f());
})(this, "module_name", (function () {
        'use strict';
        
        
        let index = {
            
        };
        return index;
    }));